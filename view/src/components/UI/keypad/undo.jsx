import React from "react";

function Undo() {
  const handleClick = () => {};

  return (
    <button
      className="undo"
      onClick={() => {
        handleClick;
      }}
    >
      Undo
    </button>
  );
}

export default Undo;

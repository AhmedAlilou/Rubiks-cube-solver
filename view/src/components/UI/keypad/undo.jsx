import React from "react";

function Undo() {
  const handleClick = () => {};

  return (
    <div
      onClick={() => {
        handleClick;
      }}
    >
      Undo
    </div>
  );
}

export default Undo;
